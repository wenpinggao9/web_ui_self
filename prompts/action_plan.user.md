用例 ID: {{case_id}}
模块路径: {{module_path}}
优先级: {{priority}}
可用角色: {{roles}}
会话模式: {{session_mode}}
当前页面 URL: {{current_url}}

前置条件:
{{preconditions}}

资源定义:
{{resources}}

说明:
- 前 {{pre_count}} 条操作步骤由前置条件解析得到, 必须保持顺序.
- 前置条件仅用于理解业务背景; 若已被展开到操作步骤中, 不要再次重复生成动作.
- 动作规划以"操作步骤"为主要来源, 以"预期结果"生成最后的断言动作.
- **可导航业务页须保留进入动作**: 步骤含「在'订单列表'页面」等侧栏可进的业务模块时, 须先规划进入该页再操作.
- **操作上下文不是菜单**: 步骤写「在详情页」「在弹窗中」或前置已写「已在XX页」, 且当前 URL/会话表明已在该上下文时, **不要**规划侧栏进入, 直接输出带位置语义的操作.
- **角色**: 同用例无角色切换时所有动作省略 role; 有切换时仅在切换后首条操作写 role, 且必须是上方「可用角色」中的 key, 依据步骤/前置执行主体推断, 禁止凭页面或按钮猜角色.

操作步骤:
{{steps}}

预期结果:
{{expectations}}

请严格只输出 {"actions": [...]} JSON, 不要输出任何解释文字.
